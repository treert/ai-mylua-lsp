---
alwaysApply: true
---

# 通用编码纪律

减少 LLM 编码常见错误的行为准则。偏向谨慎而非速度；对于 trivial 任务可自行判断。

## 1. 先想再写

**不要假设、不要隐藏困惑、主动暴露权衡。**

- 动手前先明确列出假设。不确定就问。
- 有多种解读时列出选项让用户选，不要默默挑一个。
- 存在更简单的方案时主动说出来，该 push back 就 push back。
- 有任何不清楚的地方，停下来，说明哪里不清楚，然后问。

## 2. 简洁优先

**写最少的代码解决问题，不做投机性设计。**

- 不加没人要求的功能。
- 单次使用的代码不做抽象。
- 没人要求的"灵活性""可配置性"不加。
- 不为不可能的场景写错误处理。
- 200 行能用 50 行解决的，重写。

自问：「一个资深工程师会不会说这过度设计了？」如果会，简化。

## 3. 最小改动

**只改该改的。只清理自己造成的废弃。**

编辑已有代码时：
- 不顺手"改善"不相关的代码、注释、格式。
- 不重构没有问题的代码。
- 匹配已有代码风格，即使你有不同偏好。
- 发现不相关的死代码，提醒但不删除。

自己的改动产生的废弃：
- 清理自己的改动导致的无用 import / 变量 / 函数。
- 不删除改动之前就已存在的死代码，除非被要求。

检验标准：**每一行变更都应能直接追溯到用户的请求。**

## 4. 目标驱动

**先定义可验证的成功标准，再动手。**

把模糊任务转化为可验证的目标：
- "加校验" → "写无效输入的测试，然后让测试通过"
- "修 bug" → "写复现测试，然后让测试通过"
- "重构 X" → "确保重构前后测试全绿"

多步任务先列简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
```

强成功标准让你能独立循环推进；弱标准（"弄好就行"）需要反复确认。
