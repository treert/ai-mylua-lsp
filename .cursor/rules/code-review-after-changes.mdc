---
description: 代码修改后必须自动 review
alwaysApply: true
---

# 代码修改后自动 Review

每次完成一组**功能性代码修改**后（即将告知用户"完成"之前），必须执行以下检查：

1. **编译验证** — 确保 `cargo build` / `tsc` / 相关构建命令通过，无新增 warning。
2. **逻辑自查** — 回顾本次所有改动，检查：
   - 是否有遗漏的 edge case（如空值、溢出、越界）
   - 新增代码是否与已有代码风格一致
   - 是否引入了死代码或未使用的导入
3. **调用 code-reviewer** — 对于涉及 **多文件** 或 **核心逻辑变更** 的改动，使用 `code-reviewer` subagent 进行正式 review。

不需要 review 的场景：单行修改、注释/文档变更、配置文件微调。
