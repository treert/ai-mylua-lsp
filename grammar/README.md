# grammar

**Tree-sitter** 文法工程（Lua 5.3+、EmmyLua 注释结构及后续定制扩展）。

- 目标：`tree-sitter test` 绿；生成 parser 供 `../lsp` 链接。
- 实现阶段：见仓库根目录 [docs/implementation-roadmap.md](../docs/implementation-roadmap.md) 阶段 A。

初始化 tree-sitter 工程时，可将本目录内容替换为 `tree-sitter init` 生成物，或保持本仓库约定命名（如 `tree-sitter-mylua`）并在文档中同步。
